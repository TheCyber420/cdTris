/**
 * @brief   The main method of the program, calls the necesarry functions to
 *          start running the game
 * 
 * @return  * int 
 */
#include "main_menu.h"
int main() {
    SetTargetFPS(120);
    start_main_menu();
    return 0;
}